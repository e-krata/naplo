import 'package:flutter/material.dart';

class ModifyTeacherNamesScreen extends StatelessWidget {
  const ModifyTeacherNamesScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Teachers')));
}