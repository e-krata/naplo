import 'package:flutter/material.dart';

class AppIconScreen extends StatelessWidget {
  const AppIconScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('App icon')));
}