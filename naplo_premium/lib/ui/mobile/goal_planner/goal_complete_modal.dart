import 'package:flutter/material.dart';

class GoalCompleteModal extends StatelessWidget {
  const GoalCompleteModal({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}